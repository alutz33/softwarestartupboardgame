import type { TokenColor } from '../../types';

export const TOKEN_COLORS_MAP: Record<TokenColor, string> = {
  green: 'bg-green-500',
  orange: 'bg-orange-500',
  blue: 'bg-blue-500',
  purple: 'bg-purple-500',
};

export const TOKEN_LABELS: Record<TokenColor, string> = {
  green: 'FE',
  orange: 'BE',
  blue: 'FS',
  purple: 'DO',
};
